;; doctests for beta nodes
>> (progn
     (unless (equal (package-name *package*) "MPS")
       (in-package :mps))
     NIL)
NIL
>> (clear)
T
>> (defstruct foo bar)
FOO
>> (defrule test
     (foo (bar ?bar1))
     (foo (bar ?bar2))
     =>
     (print `(,?bar1 ,?bar2)))
TEST
>> (assert-facts #S(foo :bar 1) #S(foo :bar 2))
2
>> (length (agenda))
4
